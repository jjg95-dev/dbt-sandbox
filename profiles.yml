technical_challenge:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: 'database'
      schema: 'main'
      schemas_and_paths:
        main: 'data/ops_data.db'
      schema_directory: 'data'

